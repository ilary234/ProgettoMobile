package com.example.progettoesame.data.repositories

import androidx.datastore.core.DataStore
import java.util.prefs.Preferences

class CategoryRepository(private val dataStore: DataStore<Preferences>) {
    //TODO
}